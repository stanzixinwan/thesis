\documentclass{brandeis-thesis3.3}
\usepackage[utf8]{inputenc}

\usepackage{graphicx}
\usepackage{float}
\usepackage{subcaption}

\usepackage{hyperref}
\usepackage[round]{natbib}
\usepackage{amsmath}
\usepackage{svg}


\renewcommand{\bibsection}{\chapter*{References}\addcontentsline{toc}{chapter}{References}
}

\title{The Organization of Spatiotemporal and Direction Tuning in the Primary Visual Cortex}
\author{Stan Wan}

\graduationmonth{May}
\graduationyear{2026}

\program{
  Department of Biology
}
\advisor{Stephen Van Hooser}
\degreetype{Science}

\begin{document}

\maketitlepage

\chapter*{Acknowledgments}
\addcontentsline{toc}{chapter}{Acknowledgments}

% Contain the double spacing in an environment so it doesn't affect the abstract
\begin{doublespacing}

This thesis would not have been possible without the support,
guidance, and generosity of many people, and I am grateful for
the opportunity to acknowledge them here.

First and foremost, I would like to thank my advisor, Professor
Stephen Van Hooser, for welcoming me into his laboratory and
for his patient mentorship throughout this project. Steve's
guidance has shaped not only the scientific content of this
thesis but also the way I approach research as a whole: his
willingness to spend time at the whiteboard working through a
confusing result, his insistence on letting the data lead the
interpretation, and his trust in giving me the freedom to
pursue my own analyses while always being available when I
needed direction, have been formative experiences for me as
a young scientist. 

I am deeply indebted to the members of the Van Hooser
Laboratory who collected the two-photon calcium imaging data
that this thesis is built upon. Without their careful surgical
work, imaging sessions, and the years of methodological
development that preceded my arrival in the lab, none of the
analyses in this thesis would exist. 

I am grateful to the Brandeis University Department of Biology
and the Neuroscience Program for the educational foundation
that made this project possible, and to the IACUC and animal
care staff whose work ensures that research with ferrets is
conducted humanely and rigorously.

Finally, I would like to thank my family. To my parents, 
thank you for your unwavering support of
my education and for trusting me to find my own path, even
when that path took me far from home. Your encouragement has
meant more to me than I can express in a few sentences here.


\end{doublespacing}

% Page must be manually cleared before and after abstract since it's not a section/chapter as-is
\clearpage

\begin{thesis-abstract}
\addcontentsline{toc}{chapter}{Abstract}
How the primary visual cortex (V1) encodes the dynamic properties of moving stimuli remains a central question in systems neuroscience. Classically, individual V1 neurons are characterized by their preferred direction and orientation at a single temporal frequency (TF), often with the assumption that these preferences are stable properties of the cell. This thesis investigates the stability and spatial organization of direction and orientation tuning across varying temporal frequencies in the primary visual cortex of the ferret (\textit{Mustela putorius furo}). Using in vivo two-photon calcium imaging, we recorded the population responses of V1 neurons to drifting sinusoidal gratings across multiple spatial and temporal frequencies. 

At the single-cell level, we found that preferred direction is not invariant; a substantial fraction of neurons shifted their direction preference between low (1.6 Hz) and high (6.4 Hz) temporal frequencies, with many undergoing a near-$180^{\circ}$ reversal. Contrary to prior single-unit reports, orientation preference also exhibited systematic TF-induced shifts, accompanied by a broader population-level decline in orientation selectivity. At the population level, while the macroscopic topology of the direction and orientation maps remained intact, pairwise spatial analysis revealed a weak but consistent local structure: neighboring neurons were more likely to exhibit similar TF-induced preference shifts than distant neurons. Furthermore, the magnitude of these shifts varied systematically with the spatial frequency of the stimulus. 

Together, these findings challenge the traditional view of V1 functional maps as fixed representations, demonstrating instead that direction and orientation preferences are stimulus-dependent constructs whose variability is locally organized across the cortical surface.

\end{thesis-abstract}

% Page must be manually cleared before and after abstract since it's not a section/chapter as-is
% \clearpage

\doublespacing

\tableofcontents
\clearpage

\listoffigures
\addcontentsline{toc}{chapter}{List of Figures}
\clearpage

\startbody

\chapter[Introduction]{Introduction}
How the brain transforms the continuous flux of light into a coherent
perception of a world in motion is one of the central questions in
systems neuroscience. At the core of this challenge lies the primary
visual cortex (V1), the earliest cortical stage of visual processing,
which must encode not merely static features such as contrast and
orientation but also the dynamic properties of moving stimuli:
direction, speed, and spatiotemporal structure. Understanding how V1
neurons collectively represent these multiple dimensions, and how this
representation is organized across the cortical surface, remains a
fundamental open question in sensory neuroscience.

% ----------------------------------------------------------------
\section{V1 as a Spatiotemporal Filter}
% ----------------------------------------------------------------

Early pioneering work established that V1 neurons function as
spatiotemporal filters, selectively responding to oriented edges at
specific spatial and temporal frequencies \citep{Hubel1962,
Albrecht1981}. These neurons extract local features from the visual
scene through their tuned receptive fields, forming the foundational
building blocks of visual perception. The perception of motion,
however, demands more than simple feature detection: it requires the
integration of spatial and temporal signals to extract quantities such
as velocity, which are not encoded in the raw photoreceptor output
\citep{Adelson1985}. Consequently, characterizing the spatiotemporal
tuning properties of V1 neurons and their organization across the
cortex has become a central objective in visual neuroscience.

% ----------------------------------------------------------------
\section{The Ferret as a Model System}
% ----------------------------------------------------------------

A key consideration in studying cortical organization is the choice of
experimental model. Unlike rodents, which exhibit a ``salt-and-pepper''
distribution of neuronal selectivity with no systematic spatial
organization \citep{Ohki2005, Ohki2007}, carnivores such as ferrets
and cats possess a columnar architecture in which neurons sharing
similar tuning preferences are clustered into coherent functional maps
across the cortical surface \citep{Bonhoeffer1991, Weliky1996}.
The domestic ferret (\textit{Mustela putorius furo}) is particularly
advantageous for several reasons. First, its gyrencephalic cortex
exposes broad, flat lateral gyrus that are directly accessible to optical imaging
\citep{Issa1999}. Second, the ferret's protracted postnatal visual
development provides a unique experimental window: at eye opening
(approximately postnatal day 30--33), V1 neurons are
orientation-selective but lack direction selectivity, which emerges
progressively over subsequent days of visual experience
\citep{Clemens2012, Li2008}. This developmental trajectory allows
researchers to dissect the contributions of innate connectivity and
experience-dependent plasticity to functional map formation, which is a
separation that is considerably more difficult in the mouse, where many
functional properties are established prior to or rapidly after eye
opening \citep{Yu2025}.

% ----------------------------------------------------------------
\section{The Physics of Motion: Spatial Frequency,
  Temporal Frequency, and Speed}
% ----------------------------------------------------------------

To understand how the visual cortex encodes motion, it is first
necessary to define the physical parameters of moving stimuli. The
standard probe in visual neuroscience is the drifting sinusoidal
grating, which allows visual information to be decomposed into two
orthogonal dimensions. \textbf{Spatial frequency} (SF), measured in
cycles per degree of visual angle ($\text{c/deg}$), quantifies the
density of the luminance pattern: high SF corresponds to fine textures,
while low SF corresponds to coarse, broad features. \textbf{Temporal
frequency} (TF), measured in cycles per second ($\text{Hz}$),
quantifies the rate at which the pattern modulates over time at a fixed
location.

Critically, the perceptual speed of a moving grating is not an
intrinsic property of the stimulus but a derived relationship:
%
\begin{equation}
  \text{Speed}\;(\deg/\text{s}) \;=\;
  \frac{\text{TF}\;(\text{Hz})}{\text{SF}\;(\text{c/deg})}.
  \label{eq:speed}
\end{equation}
%
This equation reveals a fundamental ambiguity inherent in motion
sensing. A neuron responding to a given temporal modulation rate cannot,
in isolation, determine whether it is driven by a fine-textured object
moving slowly or a coarse object moving rapidly. Resolving this
ambiguity requires neural mechanisms sensitive to the ratio
$\text{TF}/\text{SF}$, rather than to either dimension independently
\citep{Adelson1985}. This relationship motivates a central
question about V1 coding: if temporal and spatial frequency interact to
define speed, do V1 neurons encode direction and orientation in a manner
that is stable across changes in TF and SF, or do these fundamental
preferences themselves vary with the spatiotemporal parameters of the
stimulus?

% ----------------------------------------------------------------
\section{Functional Architecture of Ferret V1:
  Orientation and Direction Maps}
% ----------------------------------------------------------------

The columnar organization of ferret V1 is defined by two superimposed
large-scale functional maps. The \textbf{orientation preference (OP)
map} describes the orderly arrangement of neurons tuned to specific
edge orientations across the cortical surface \citep{Bonhoeffer1991}.
Within this map, iso-orientation domains, where extended cortical patches
where neuronal orientation preference changes gradually, are punctuated
by topological singularities known as \textit{pinwheel centers}, at
which all orientations converge at a single locus. These singularities
are a mathematical necessity in any continuous map of a cyclic variable
\citep{Ohki2006}. Neurons proximal to pinwheel centers tend to exhibit
broader tuning and lower orientation selectivity compared to those in
iso-orientation domains, reflecting heterogeneous input convergence at
these singularities \citep{Nauhaus2008}.

Superimposed upon the orientation map is the \textbf{direction
selectivity map}. Because direction is a vector quantity (i.e. leftward and
rightward motion are distinct) direction maps are characterized by
sharp ``fractures'': lines across the cortex at which the preferred
direction abruptly reverses by $180^{\circ}$. In ferret V1, these
fractures typically align with the centers of iso-orientation domains,
partitioning each domain into two sub-regions preferring opposite
directions of motion \citep{Weliky1996, Clemens2012}. This tessellation
ensures comprehensive coverage of the orientation--direction feature
space within any local cortical module. Recent functional ultrasound
imaging has further revealed that orientation domains are not simple
cylinders but adopt a cone-shaped geometry in three dimensions, with
implications for how tuning properties are integrated across cortical
layers \citep{Hu2023}.

% ----------------------------------------------------------------
\section{Stability of Direction and Orientation Preference
  Across Stimulus Conditions}
% ----------------------------------------------------------------

The orientation and direction maps described above are typically
characterized at a single, fixed spatiotemporal frequency. An implicit
assumption underlying much of this literature is that the preferences
revealed by such measurements are stable properties of V1 neurons---that
a cell tuned to rightward motion at one temporal frequency will remain
tuned to rightward motion at another. This assumption, however, has
been directly challenged by experimental evidence.

Electrophysiological recordings in ferret V1 demonstrated that the
preferred direction of motion for a substantial fraction of neurons
shifts substantially when temporal frequency is varied
\citep{Moore2005}. Some cells reverse their preferred direction
entirely: responding preferentially to rightward motion at low TF but
leftward motion at high TF. This finding implies that the direction map,
as measured at a single TF, may be an incomplete or misleading
description of the true directional tuning landscape of V1. In the same
study, orientation preference was reported to be comparatively stable
across TF, leading to the conclusion that orientation and direction are
encoded through partially dissociable mechanisms in V1 \citep{Moore2005}.

These findings, however, were established using single-unit
electrophysiology in anesthetized cats, which permits only sparse
sampling of the neural population and does not resolve the spatial
organization of TF-dependent preference shifts across the cortical
surface. Whether similar phenomena occur in ferret V1, whether
orientation preference is truly invariant to TF changes or whether
earlier conclusions reflected limited sampling, and whether neurons
with large or small preference shifts are spatially clustered or
intermingled on the cortical map, remain open questions that the
present study is positioned to address.

% ----------------------------------------------------------------
\section{Speed Tuning in V1: Motivation and Context}
% ----------------------------------------------------------------

Beyond the question of preference stability, the joint encoding of SF
and TF by V1 neurons is relevant to a deeper computational question:
whether V1 encodes stimulus speed. A longstanding view held that
explicit speed tuning (\textit{i.e.}, the encoding of object velocity invariant to the
spatial pattern of the stimulus) was a property of extrastriate areas,
most prominently the middle temporal area (MT/V5), rather than V1
itself. Under this view, V1 neurons were modeled as spatiotemporally
separable filters: the preferred SF and preferred TF were considered
independent parameters, such that a neuron's preferred speed varied with
the spatial structure of the stimulus \citep{Priebe2006}.

However, this view has been substantially revised. Electrophysiological
studies in macaque V1 identified a population of complex cells whose
spatiotemporal tuning is \textit{inseparable}: as the SF of the
stimulus increases, the preferred TF increases proportionally,
maintaining a constant preferred speed across spatial patterns
\citep{Priebe2006}. Recent large-scale two-photon calcium imaging from
our laboratory has further demonstrated that speed-tuned neurons in
ferret V1 are not randomly distributed but form spatially clustered
domains \citep{Casanova2025}, establishing that a functional map for
speed exists within V1 itself. The question of how such speed-selective
organization relates to the direction and orientation maps,
particularly how TF-dependent shifts in direction preference might
reflect or predict the emergence of speed tuning at the population
level, which provides important motivation for the present work, though its
full characterization lies beyond the scope of this thesis.

% ----------------------------------------------------------------
\section{Research Gaps and Specific Aims}
% ----------------------------------------------------------------

Although the orientation and direction architecture of ferret V1 is well
established at the map level \citep{Weliky1996, Bonhoeffer1991,
Ohki2006}, several critical gaps remain in our understanding of how
these maps behave across varying stimulus conditions. The
TF-dependence of direction and orientation preference has been
demonstrated in cat using sparse single-unit recordings, but has not
been examined in ferret V1, has not been observed at the single-cell
population resolution afforded by two-photon calcium imaging, and has
never been analyzed in terms of the spatial organization of TF-induced
preference shifts across the cortical map. It is unknown whether cells
that change their direction preference with TF are spatially clustered
or randomly interspersed among stable cells, whether the overall map
topology is preserved across TF conditions, and whether orientation
preference, considered TF-invariant by previous studies \citep{Moore2005}, 
shows systematic shifts under the broader stimulus conditions examined here.

This thesis addresses these gaps through computational analysis of
large-scale two-photon calcium imaging data acquired from the ferret primary
visual cortex, using drifting grating stimuli spanning multiple
directions, spatial frequencies, and temporal frequencies. Two specific
aims are pursued:

\begin{enumerate}
  \item\textbf{Aim~1.}
    \label{aim:cellevel}
    To characterize, at the level of individual neurons, how preferred
    direction and preferred orientation change as a function of temporal
    frequency across a range of spatial frequencies. Specifically, to
    quantify the fraction of cells exhibiting large TF-dependent
    direction shifts and to determine whether orientation preference, 
    contrary to prior reports, also shifts systematically with TF.

  \item\textbf{Aim~2.}
    \label{aim:maplevel}
    To examine whether TF-induced preference shifts are spatially
    organized on the cortical surface. Specifically, to determine
    whether the large-scale topology of direction and orientation maps
    is preserved across temporal frequencies, whether cells with large
    preference shifts are spatially clustered or intermixed with stable
    cells, and whether the magnitude of TF-induced shifts is
    predictable from a cell's location relative to established map
    features.
\end{enumerate}

Together, these aims provide a population-level, spatially resolved
characterization of how temporal frequency modulates the directional
and orientational tuning landscape of ferret V1. The findings
contribute to a revised understanding of V1 functional maps, not as
fixed representations of feature preferences, but as stimulus-dependent
structures whose organization may reflect the underlying spatiotemporal
computations performed by local cortical circuits.

\chapter[Verbose Version]{Verbose Version: The Effect of Temporal Frequency on Direction and Orientation Tuning}
The primary visual cortex (V1) is the first cortical region to
process visual information, and a central question in visual
neuroscience is how its neurons represent the direction and
orientation of moving objects. For decades, V1 has been
characterized by measuring each neuron's preferred direction
and orientation at a single spatial and temporal frequency, treating these
preferences as fixed properties of the cell. Prior work in
anesthetized ferrets had already hinted that preferred direction
can change when the temporal frequency (TF) of a drifting
grating (how quickly the stimulus pattern moves past a fixed
point in space) is varied, but orientation preference was
reported to be invariant. Whether these observations can be seen at the scale of entire
neuronal populations rather than a handful of recorded cells,
and whether the cells that change their preferences are
distributed randomly or in organized clusters across the
cortex, were all open questions.

This thesis uses two-photon calcium imaging data from ferret
V1--a technique that allows simultaneous recording from
hundreds of neurons at single-cell resolution--to ask how
direction and orientation preferences change when TF is varied,
and whether those changes are spatially organized. At the
single-cell level, a substantial fraction of neurons changed
their preferred direction when TF was increased from 1.6\,Hz to
6.4\,Hz, with many cells reversing their preferred direction by
nearly 180$^\circ$. Orientation preference, previously assumed
to be stable across TF, also shifted systematically in a
non-negligible fraction of cells, which is a finding that complicates
the earlier view of orientation tuning as a TF-invariant
property of V1. At the population level, the large-scale
topology of both the direction and orientation maps was
preserved across TF conditions, consistent with the classical
columnar organization of ferret V1. However, closer analysis
revealed that cells with similar TF-induced preference shifts
tended to be spatially clustered: neighboring neurons shifted
their preferences by similar amounts more often than distant
neurons did, indicating a weak but consistent spatial structure
to TF-induced retuning. These effects varied with the spatial
frequency of the stimulus, suggesting that the instability of
direction and orientation preferences across TF is itself shaped
by the spatiotemporal parameters of the visual input. Together,
the results suggest that V1 functional maps are not rigid
representations of fixed feature preferences, but stimulus-dependent
structures whose local organization reflects the underlying
spatiotemporal computations performed by cortical circuits.

% ================================================================
%  CHAPTER: Methods
%  Thesis — Spatiotemporal Tuning in Ferret V1
%  Data acquisition by Van Hooser Lab; all analyses by the author.
% ================================================================

\chapter{Methods}

% ----------------------------------------------------------------
\section{Animals}
% ----------------------------------------------------------------

All experimental procedures were approved by the Brandeis
University Institutional Animal Care and Use Committee (IACUC)
and performed in accordance with the National Institutes of
Health \textit{Guide for the Care and Use of Laboratory Animals}.
Data were collected from 7 juvenile ferrets
(\textit{Mustela putorius furo}; postnatal day 52--71;
3 litters) obtained from Marshall Farms (North Rose, NY).
Animals were housed on a 12:12-h light/dark cycle with
\textit{ad libitum} access to food and water prior to
experiments. A summary of all animals and recording sessions
is provided in Table~\ref{tab:animals}.

\begin{table}[htbp]
  \centering
  \caption[Summary of animals and recording sessions.]{Summary of animals and recording sessions. Virus
    injection dates and imaging dates are listed for each
    animal. Age refers to postnatal day at the time of imaging.\\}
  \label{tab:animals}
  \begin{tabular}{lcccc}
    \hline
    Animal (Litter) & Age (days) & Weight (g)
      & Virus injection & Imaging date \\
    \hline
    Ferret 356       & 71 & 661 & 2019-10-09 & 2019-11-12 \\
    Ferret 363 (A)   & 52 & 410 & 2020-08-03 & 2020-08-21 \\
    Ferret 363 (B)   & 55 & 310 & 2020-08-04 & 2020-08-24 \\
    Ferret 363 (C)   & 58 & 440 & 2020-08-04 & 2020-08-27 \\
    Ferret 365 (A)   & 60 & 440 & 2020-08-10 & 2020-09-04 \\
    Ferret 365 (B)   & 64 & 450 & 2020-08-11 & 2020-09-08 \\
    Ferret 365 (C)   & 67 & 540 & 2020-08-10 & 2020-09-11 \\
    \hline
  \end{tabular}
\end{table}

% ----------------------------------------------------------------
\section{Data Acquisition}
\label{sec:data_acquisition}
% ----------------------------------------------------------------

All surgical procedures and two-photon imaging were performed
by members of the Van Hooser Laboratory; detailed protocols
have been described previously
\citep{VanHooser2015, Smith2016}. Briefly, each ferret
received an injection of an adeno-associated virus (AAV)
carrying the genetically encoded calcium indicator GCaMP6s
into cortical layer~2/3 of primary visual cortex (V1)
18--34~days prior to imaging
(Table~\ref{tab:animals}). On the day of imaging, a
craniotomy was performed over V1 and a glass cranial window
was implanted to provide optical access
(Figure~\ref{fig:experimental_setup}A).

In vivo two-photon calcium imaging was performed using a
Brucker IV two-photon microscope equipped with a
Nikon 16$\times$/0.8~NA water-immersion objective. GCaMP6s
fluorescence was excited at 920~nm and images were acquired
at approximately 1~Hz with a field of view of approximately
[X~$\times$~Y]~$\mu$m.

The computational analyses presented in this thesis
including cell identification, extraction of tuning curves,
and all spatial analyses were performed by the author.

% ----------------------------------------------------------------
\section{Visual Stimuli}
\label{sec:stimuli}
% ----------------------------------------------------------------

Visual stimuli were generated using custom MATLAB code with
the Psychophysics Toolbox
\citep[Psychtoolbox-3;][]{Brainard1997, Pelli1997} and
displayed on a gamma-corrected monitor positioned
25 cm from the contralateral eye.
The ipsilateral eye was occluded with an opaque patch.

Stimuli consisted of full-field drifting sinusoidal gratings
presented at [100]\% contrast. Gratings were shown at 8
equally spaced directions ($0^\circ$, $45^\circ$, $90^\circ$,
$135^\circ$, $180^\circ$, $225^\circ$, $270^\circ$,
$315^\circ$), 5 spatial frequencies (0.04, 0.08, 0.15, 0.32,
and 0.64~cyc/deg), and 5 temporal frequencies (0.5, 1.6, 2.4,
6.4, and 12.8~Hz). (Figure~\ref{fig:experimental_setup}B) Each 
unique stimulus condition (direction~$\times$~SF~$\times$~TF) 
was presented for 5~s, followed by a mean-luminance gray
screen inter-stimulus interval of 5~s. Each
condition was repeated 7–8 times in a pseudo-random order. A
blank (mean-luminance) condition of equal duration was
interleaved to serve as a baseline.


\begin{figure}[htbp]
  \centering
  \includegraphics[width=\linewidth]{figures/_ferret_stimulus.pdf}
  \caption[Experimental setup and visual stimulus design.]{\textbf{Experimental setup and visual stimulus design.} 
    (\textit{A})~Schematic of the two-photon calcium imaging
    preparation. A cranial window was implanted over ferret V1,
    and neuronal activity was recorded with a two-photon
    microscope while the animal viewed visual stimuli on a
    monitor positioned contralateral to the imaged hemisphere.
    (\textit{B})~Stimulus parameter space. Drifting sinusoidal
    gratings were presented at 8 directions, 5 spatial
    frequencies (0.04--0.64~cyc/deg), and 5 temporal frequencies
    (0.5--12.8~Hz). Not all SF~$\times$~TF combinations were
    tested in every session.}
  \label{fig:experimental_setup}
\end{figure}



Not all SF~$\times$~TF combinations were presented in every
recording session. In some sessions, only a subset of spatial
frequencies was tested (as few as a single SF =
0.15~cyc/deg), and one session included an additional
condition at TF = 3~Hz with SF = 0.15~cyc/deg. Cells recorded
with fewer than the full complement of spatial frequencies
were excluded from analyses requiring across-SF comparisons.
The typical stimulus set for analysis comprised
5~SF~$\times$~5~TF~$\times$~8~directions = 200 unique
conditions.



% ----------------------------------------------------------------
\section{Image Processing and Cell Identification}
\label{sec:image_processing}
% ----------------------------------------------------------------

Acquired image sequences were processed and analyzed using
\texttt{twophotonbulkload}, a MATLAB graphical user interface
developed by the Van Hooser Laboratory
(\url{https://github.com/VH-Lab/vhlab-TwoPhoton-matlab}).
Motion correction was performed via rigid registration to a
time-averaged reference frame.

Regions of interest (ROIs) corresponding to individual cell
bodies were identified manually by an experienced observer
using the \texttt{twophotonbulkload} GUI
(Figure~\ref{fig:example_cells}). ROIs were drawn as circles
with a diameter of approximately 8 pixels, matched to the
typical soma size at the imaging magnification used. Cells
were selected based on clearly identifiable somata in the
time-averaged fluorescence image; non-somatic signals (e.g.,
dendrites, blood vessels, and neuropil-dominated regions)
were excluded. All ROI data were saved in the Neuroscience
Data Interface (NDI) format \citep{GarciaMurillo2022}.

For each ROI, the raw fluorescence time series $F(t)$ was
extracted as the mean pixel intensity within the ROI at each
frame. The fractional change in fluorescence was then
calculated as
%
\begin{equation}
  \frac{\Delta F}{F_0}(t) \;=\;
    \frac{F(t) - F_0}{F_0},
  \label{eq:dff}
\end{equation}
%
where $F_0$ is the baseline fluorescence, defined as the
[mean fluorescence during blank (mean-luminance) epochs /
mean of the pre-stimulus period for each trial].

The spatial coordinates $(x_i, y_i)$ of each neuron were
recorded as the centroid of its ROI in the imaging field,
expressed in pixels. These coordinates were used for all
subsequent spatial analyses.


\begin{figure}[htbp]
  \centering
  \includegraphics[width=\linewidth]{figures/_twophoton_responses.pdf}
  \caption[Two-photon calcium imaging of neuronal responses in
    ferret visual cortex.]{\textbf{Two-photon calcium imaging of neuronal responses in
    ferret visual cortex.}
    (\textit{A})~Representative time-averaged fluorescence image
    of a two-photon FOV in ferret V1. Cell bodies were manually
    identified using the \texttt{twophotonbulkload} GUI and are
    outlined as ROIs.
    (\textit{B})~Example direction tuning curves for
    two representative cells at selected SF~$\times$~TF
    conditions, illustrating the diversity of tuning profiles
    observed in the population. Error bars indicate
    $\pm$~SEM across trials.}
  \label{fig:example_cells}
\end{figure}


% ----------------------------------------------------------------
\section{Data Analysis}
\label{sec:analysis}
% ----------------------------------------------------------------

All analyses were performed in MATLAB R2024b
(MathWorks, Natick, MA) using custom scripts. Analysis code is available at \url{https://github.com/VH-Lab/vhlab-tpdirection-matlab} upon request.

% ---- 2.6.1  Visual responsiveness ----
\subsection{Visual Responsiveness}
\label{sec:responsiveness}

To identify visually responsive neurons, a one-way analysis of
variance (ANOVA) was performed for each cell, testing whether
the mean $\Delta F/F_0$ differed significantly across stimulus
conditions (including the blank condition). Cells were
classified as visually responsive if $p \leq 0.05$. Only
responsive cells were included in subsequent analyses. Across
all animals and FOVs, a total of 3772 neurons were imaged, of
which 2362 (62.6\%) met the responsiveness criterion and were
retained for analysis.

% ---- 2.6.2  Tuning curves ----
\subsection{Direction and Orientation Tuning Curves}

For each neuron and each SF~$\times$~TF combination, a
direction tuning curve was constructed by computing the mean
$\Delta F/F_0$ response across trials for each of the 8
directions. The orientation tuning curve was derived by
collapsing opposite directions (i.e., averaging responses at
$\theta$ and $\theta + 180^\circ$), yielding 4 orientation
conditions.

% ---- 2.6.3  Selectivity indices ----
\subsection{Direction and Orientation Selectivity Indices}

The direction selectivity index (DSI) was computed as the
normalized length of the mean resultant vector in direction
space:
%
\begin{equation}
  \text{DSI} \;=\;
    \frac{\left|\sum_{k} R_k \, e^{i\theta_k}\right|}
         {\sum_{k} R_k},
  \label{eq:dsi}
\end{equation}
%
where $R_k$ is the mean response to direction $\theta_k$ (in
radians), $i$ is the imaginary unit, and the sum runs over all
8 directions. DSI ranges from 0 (equal response to all
directions) to 1 (response to only one direction).

The orientation selectivity index (OSI) was computed
analogously, but with the direction angles doubled to map
orientation into a full circle:
%
\begin{equation}
  \text{OSI} \;=\;
    \frac{\left|\sum_{k} R_k \, e^{i \cdot 2\theta_k}\right|}
         {\sum_{k} R_k}.
  \label{eq:osi}
\end{equation}
%
DSI and OSI were computed separately at each SF~$\times$~TF
condition for every responsive cell.

% ---- 2.6.4  Preferred direction / orientation ----
\subsection{Preferred Direction and Orientation}

The preferred direction of each neuron at a given
SF~$\times$~TF condition was defined as the angle of the mean
resultant vector:
%
\begin{equation}
  \theta_{\text{pref}} \;=\;
    \arg\!\left(\sum_{k} R_k \, e^{i\theta_k}\right).
  \label{eq:prefdir}
\end{equation}
%
Similarly, the preferred orientation was computed as
%
\begin{equation}
  \phi_{\text{pref}} \;=\;
    \tfrac{1}{2}\,\arg\!\left(\sum_{k} R_k \,
      e^{i \cdot 2\theta_k}\right),
  \label{eq:prefori}
\end{equation}
%
which yields a value in the range $[0^\circ, 180^\circ)$.

% ---- 2.6.5  Delta-preference ----
\subsection{Shift in Preferred Direction and Orientation
  across Temporal Frequency}
\label{sec:delta_pref}

To quantify how feature preference changed with temporal
frequency, we computed the absolute shift in preferred
direction between two reference TF conditions. Unless
otherwise stated, the low-TF reference was 1.6~Hz and the
high-TF reference was 6.4~Hz. These two values were chosen
because they consistently elicited robust responses across
recorded neurons and were separated by a sufficiently large
interval to reveal TF-dependent changes in
tuning, while both being present in all recording sessions.

The absolute direction preference shift was defined as
%
\begin{equation}
  |\Delta\text{direction}| \;=\;
    \min\!\bigl(|\theta_{\text{pref}}^{\text{high}}
      - \theta_{\text{pref}}^{\text{low}}|,\;
    360^\circ - |\theta_{\text{pref}}^{\text{high}}
      - \theta_{\text{pref}}^{\text{low}}|\bigr),
  \label{eq:delta_dir}
\end{equation}
%
yielding a value in $[0^\circ, 180^\circ]$. An analogous
quantity, $|\Delta\text{orientation}|$, was computed using
preferred orientations and wrapped to $[0^\circ, 90^\circ]$.

These metrics were computed for all responsive cells at each
of the 5 spatial frequencies independently, to assess whether
the magnitude or prevalence of preference shifts depended on
SF.

% ---- 2.6.6  Functional maps ----
\subsection{Functional Preference Maps}

To visualize the spatial organization of feature preferences,
each neuron was plotted at its $(x_i, y_i)$ coordinates in the
FOV and color-coded by its preferred direction (using a
circular colormap spanning $0^\circ$--$360^\circ$) or preferred
orientation ($0^\circ$--$180^\circ$). Separate maps were
generated for each TF condition to allow visual comparison of
map topology across temporal frequencies.

Preference-shift maps were constructed by plotting
$|\Delta\text{direction}|$ as a vector at each cell's
location, with vector length proportional to the magnitude of
the shift and vector angle indicating the direction of the
shift. These maps were used to qualitatively assess whether
large preference shifts were spatially clustered or uniformly
distributed across the FOV.

% ---- 2.6.7  Pairwise spatial analysis ----
\subsection{Pairwise Spatial Analysis of Preference Shifts}
\label{sec:pairwise}

To quantify the spatial structure of preference shifts, we
computed pairwise statistics across all cell pairs within each
FOV. For each pair of neurons $i$ and $j$, we calculated the
Euclidean distance $d_{ij}$ between their spatial coordinates
(in pixels) and the absolute difference in their direction
preference shifts,
$|\Delta\text{dir}_i - \Delta\text{dir}_j|$.

These pairwise measurements were binned by inter-cell distance
into 20 equally spaced bins spanning 0 to 500 pixels, and the
mean $|\Delta\text{shift}|$ was computed within each distance
bin. This analysis tests whether nearby cells undergo more
similar preference shifts than distant cells, which would
indicate a spatially organized mechanism underlying
TF-dependent retuning.

% ================================================================
\chapter[Results]{Results}
% ================================================================

To characterize how direction and orientation preference in ferret
primary visual cortex depend on temporal frequency, we analyzed
two-photon calcium imaging data acquired from \textit{7} ferrets
(Table~\ref{tab:animals}), focusing on the response of individual
neurons to drifting sinusoidal gratings varying in direction (8
equally spaced angles), spatial frequency (SF; 0.04--0.64\,c/deg),
and temporal frequency (TF; 0.5--12.8\,Hz). All analyses were
restricted to neurons that passed a one-way ANOVA criterion
($p \leq 0.05$) for direction or orientation selectivity at the
relevant TF condition. Unless stated otherwise, the TF comparison
shown is between 1.6\,Hz and 6.4\,Hz. These two values were
selected because they reliably elicited strong responses across
the recorded population while being separated by a sufficiently
large interval (a factor of four) to expose any TF-dependent
changes in tuning.


% ----------------------------------------------------------------
\section{Single-cell tuning across the SF-TF parameter space}
% ----------------------------------------------------------------

As a first characterization of the response structure, we visualized
the full direction-by-TF response surface for individual neurons
across all five SF conditions (Figure~\ref{fig:surf}). For each
cell, normalized response amplitude (0--1, scaled per cell) is
displayed as a two-dimensional heatmap with direction along the
horizontal axis (0--360$^\circ$, 8 directions) and TF along the
vertical axis (0.5, 1.6, 2.4, 6.4, 12.8 Hz).

\begin{figure}[htbp]
  \centering
  \includegraphics[width=\textwidth]{figures/surf_DirTF_perSF_5cell}
  \caption[Direction$\times$TF response surfaces for five example
           neurons]{%
    \textbf{Direction-by-TF response surfaces for five representative
    neurons across spatial frequencies.}
    Each row corresponds to a single neuron; each column to one of
    five spatial frequencies (SF\,=\,0.04, 0.08, 0.15, 0.32, and
    0.64\,c/deg, left to right). Within each panel, the horizontal
    axis denotes stimulus direction (0--360$^\circ$, 8 conditions)
    and the vertical axis denotes temporal frequency (0.5, 1.6, 2.4, 
    6.4, 12.8 Hz). Color encodes normalized $\Delta F/F$ response (0--1,
    independently scaled per neuron). Several features are apparent.
    First, responses are sparse: individual neurons respond strongly
    to only a small subset of SF--TF combinations. Second, the
    preferred direction (column of peak response) is not fixed across
    TF levels; in several cells (e.g., Cell\,28, Cell\,332), the
    peak shifts to a different direction column at high TF compared
    to low TF. Third, some neurons show negligible responses at
    certain SF values across all TF and direction conditions,
    reflecting the well-known SF bandwidth of V1 neurons.}
  \label{fig:surf}
\end{figure}

Consistent with prior characterizations of V1 responses using
drifting gratings \citep{Priebe2006}, neurons exhibited tuned
responses that were narrowly concentrated in the
direction-TF parameter space. Notably, the preferred direction
was not invariant across TF levels in a substantial fraction of
cells, foreshadowing the population-level analysis described
below. Cell-to-cell variability was considerable: some neurons
responded across multiple TF levels while remaining
direction-selective, whereas others produced significant responses
at only a narrow range of SF-TF combinations.


% ----------------------------------------------------------------
\section{Direction and orientation selectivity as a function
  of temporal frequency}
% ----------------------------------------------------------------

To quantify how the \textit{degree} of direction and orientation
selectivity varied with TF, we computed the Direction Selectivity
Index (DSI) and Orientation Selectivity Index (OSI) for each
neuron at each tested TF, separately for each SF condition, and
visualized the joint distribution as two-dimensional density
heatmaps (Figure~\ref{fig:dsi_osi_tf}).

\begin{figure}[htbp]
  \centering
  \includegraphics[width=\textwidth]{figures/_DSI_OSI_vs_TF}
  \caption[Population DSI and OSI as a function of TF]{%
    \textbf{Population-level direction selectivity index (DSI) and
    orientation selectivity index (OSI) as a function of temporal
    frequency, across spatial frequencies.}
    Top row: joint distribution of DSI (vertical axis, 0--1) and TF
    (horizontal axis, Hz) for neurons passing the ANOVA threshold
    ($p \leq 0.05$), separately for each SF condition
    (0.04--0.64\,c/deg, left to right). Bottom row: analogous plots
    for OSI. Color encodes the percentage of cells in each 2D bin
    (colorbar, right). DSI distributions remain broadly similar
    across TF levels, with the bulk of cells concentrated at
    low-to-moderate DSI values (0.1--0.4) regardless of TF or SF
    condition. In contrast, OSI distributions show a clear
    TF-dependent decline: at low TF (0.5--2.4\,Hz), a substantial
    fraction of cells exhibits OSI values in the 0.4--0.7 range,
    whereas at high TF ($\geq$\,6.4\,Hz) the population
    progressively concentrates toward lower OSI values
    (0.2--0.4). This pattern is visible across all SF conditions.
    Together, these results indicate that orientation selectivity
    in ferret V1 weakens systematically as TF increases, while
    direction selectivity remains comparatively stable across the
    tested TF range.}
  \label{fig:dsi_osi_tf}
\end{figure}

These results reveal an asymmetry in how direction and orientation
selectivity depend on temporal frequency. DSI distributions were
largely TF-invariant: the population of direction-selective cells
remained similarly distributed across TF levels within each SF
condition, with no systematic shift in the modal DSI value. In
contrast, OSI showed a clear monotonic decline with increasing
TF: cells that were strongly orientation-selective at low TF
(0.5--2.4\,Hz) became progressively less selective at high TF
($\geq$\,6.4\,Hz), with the bulk of the OSI distribution shifting
toward lower values. This TF-dependent weakening of orientation
selectivity was consistent across all five SF conditions tested.
The finding that orientation \textit{tuning strength} itself
varies with TF is relevant to the interpretation of the
orientation preference shift analyses described below: any
TF-induced change in preferred orientation must be considered
in the context of a population whose orientation tuning is
itself becoming broader at high TF.


% ----------------------------------------------------------------
\section{Direction preference maps across temporal frequencies}
% ----------------------------------------------------------------

To examine whether the spatial organization of direction
preference is maintained across TF, we constructed single-cell
direction preference maps at 1.6 and 6.4\,Hz (SF\,=\,0.15\,c/deg)
from the two-photon cell coordinates, with each neuron
represented as an arrow pointing along its preferred motion
direction (Figure~\ref{fig:dir_maps}). Both individual-TF maps
and their overlay are shown to facilitate direct comparison of
map topology across temporal frequencies.

At 1.6\,Hz, the map exhibited the hallmark features of ferret V1
direction organization: smooth gradients of preferred direction
across the cortical surface, punctuated by sharp fracture lines
at which the preferred direction reversed by
$\sim$180$^\circ$, consistent with prior characterizations using
intrinsic signal imaging \citep{Weliky1996}. The
6.4\,Hz map retained this broad topographic structure but
displayed substantially more local heterogeneity: in several
regions, neighboring cells that shared a common preferred
direction at 1.6\,Hz pointed in markedly different directions at
6.4\,Hz. To quantify this population-level change, we computed
polar histograms of preferred direction at each TF
(Figure~\ref{fig:dir_radar}). The modal preferred direction
shifted between the two TF conditions, and the 6.4\,Hz
distribution was visibly broader than the 1.6\,Hz distribution,
confirming that the change visible in the maps reflects a genuine
population-level reorganization rather than noise in individual
cells.

\begin{figure}[htbp]
  \centering
  \begin{subfigure}[]{\textwidth}
    \centering
    \includegraphics[width=0.8\textwidth]{figures/_dirMap_separated.pdf}
    \caption{Single-TF maps at 1.6\,Hz (left) and 6.4\,Hz (right).}
    \label{fig:dir_map_singleTF}
  \end{subfigure}
  \begin{subfigure}[]{\textwidth}
    \centering
    \includegraphics[width=0.6\textwidth]{figures/_dirMap_overlapped.pdf}
    \caption{Overlay of both TF conditions at identical cortical
    coordinates.}
    \label{fig:dir_map_overlap}
  \end{subfigure}
  \caption[Direction preference maps at low and high TF]{%
    \textbf{Direction preference maps at two temporal frequencies}
    (SF\,=\,0.15\,c/deg, $n$\,=\,328 cells). Each cell is
    represented by an arrow oriented along its preferred motion
    direction (0--360$^\circ$), positioned at its cortical location
    (pixels). At 1.6\,Hz, smooth gradients of direction preference
    are interspersed with sharp fracture lines where the preferred
    direction reverses by $\sim$180$^\circ$, consistent with the
    known direction map architecture in ferret V1
    \citep{Weliky1996, Clemens2012}. The 6.4\,Hz map retains the
    same broad topographic structure but shows more local
    heterogeneity: in several regions, neighboring cells that
    agree at 1.6\,Hz point in markedly different directions at
    6.4\,Hz.}
  \label{fig:dir_maps}
\end{figure}

\begin{figure}[htbp]
  \centering
  \includegraphics[width=\textwidth]{figures/_dirMap_radar.pdf}
  \caption[Direction preference polar histograms at low and high TF]{%
    \textbf{Population distributions of preferred direction at
    1.6\,Hz and 6.4\,Hz} (SF\,=\,0.15\,c/deg). Polar histograms
    (32 angular bins) summarize the preferred direction of every
    responsive cell at each TF condition. The modal preferred
    direction shifts between 1.6 and 6.4\,Hz, and the 6.4\,Hz
    distribution is visibly broader, confirming at the population
    level the local heterogeneity visible in
    Figure~\ref{fig:dir_maps}.}
  \label{fig:dir_radar}
\end{figure}


% ----------------------------------------------------------------
\section{Orientation preference maps across temporal frequencies}
% ----------------------------------------------------------------

To examine whether the spatial organization of orientation
preference is maintained across TF, we constructed orientation
preference maps at 1.6\,Hz and 6.4\,Hz (SF\,=\,0.15\,c/deg) from
the two-photon cell coordinates, visualizing both the
individual-TF maps and their overlay
(Figure~\ref{fig:ori_maps}), and summarized the population-level
distributions as polar histograms
(Figure~\ref{fig:ori_radar}).

The orientation maps at low and high TF preserved the hallmark
features of ferret V1 organization: smooth gradients of
orientation preference across cortical space, consistent with
the well-described columnar architecture of carnivore visual
cortex \citep{Bonhoeffer1991, Weliky1996}. Despite the preservation of
large-scale map topology, the two TF conditions were not
identical: local disagreements between the 1.6\,Hz and 6.4\,Hz
orientation assignments were visible throughout the field of
view, and the population histograms
(Figure~\ref{fig:ori_radar}) revealed a systematic shift in the
modal preferred orientation between the two conditions. Together,
these observations indicate that individual cells contribute
differently to the orientation map depending on the temporal
frequency of stimulation, and that the population-level
orientation preference---often assumed to be TF-invariant
\citep{Moore2005}---is not fully stable across TF in ferret V1.

\begin{figure}[htbp]
  \centering
  \begin{subfigure}[]{\textwidth}
    \centering
    \includegraphics[width=0.8\textwidth]{figures/_oriMap_separated.pdf}
    \caption{Single-TF maps at 1.6\,Hz (left) and 6.4\,Hz (right).
    }
    \label{fig:ori_map_singleTF}
  \end{subfigure}
  \begin{subfigure}[]{\textwidth}
    \centering
    \includegraphics[width=0.6\textwidth]{figures/_oriMap_overlapped.pdf}
    \caption{Overlay of both TF conditions at identical cortical
    coordinates. }
    \label{fig:ori_map_overlap}
  \end{subfigure}
  \caption[Orientation preference maps at low and high TF]{%
    \textbf{Orientation preference maps at two temporal
    frequencies} (SF\,=\,0.15\,c/deg, $n$\,=\,328 cells).
    \ref{fig:ori_map_singleTF}: Each cell is represented by a line segment oriented parallel
    to its preferred orientation, positioned at its cortical
    location (pixels).
    \ref{fig:ori_map_overlap}: Cells in spatially proximal regions tend to share
    similar preferred orientations at both TFs, confirming a
    smooth orientation map at single-cell resolution
    \citep{Ohki2006}. While the gross map structure is preserved,
    local disagreements between the two TF conditions are visible
    throughout the field of view.
    }
  \label{fig:ori_maps}
\end{figure}

\begin{figure}[htbp]
  \centering
  \includegraphics[width=\textwidth]{figures/_oriMap_radar.pdf}
  \caption[Orientation preference polar histograms at low and high TF]{%
    \textbf{Population distributions of preferred orientation at
    1.6\,Hz and 6.4\,Hz} (SF\,=\,0.15\,c/deg). Polar histograms
    (16 angular bins, $-90^\circ$ to $90^\circ$) summarize the
    preferred orientation of every responsive cell at each TF
    condition. A systematic shift in the modal orientation is
    apparent between 1.6 and 6.4\,Hz, indicating that
    population-level orientation preference is not fully invariant
    to TF.}
  \label{fig:ori_radar}
\end{figure}


% ----------------------------------------------------------------
\section{Spatial structure of TF-induced preference shifts}
% ----------------------------------------------------------------

To directly examine whether TF-induced preference changes carry
spatial structure on the cortical surface, we constructed aligned
delta-vector maps for both direction and orientation. For each
neuron, the preferred direction (or orientation) at the reference
TF (1.6\,Hz) was rotated to a common reference angle of
0$^\circ$, displayed as a grey arrow; the identical rotation was
then applied to that neuron's comparison-TF (6.4\,Hz) preference,
shown as a colored arrow whose angle encodes the signed shift
relative to 0$^\circ$. This procedure removes each cell's
absolute reference preference, making the spatial pattern of
TF-induced \textit{relative} shifts directly readable from the
map. Accompanying polar histograms summarize the population-level
distribution of shift angles.


\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.6\textwidth]{figures/aligned_dirMap}
  \caption[Aligned delta-vector map: direction preference]{%
    \textbf{Aligned delta-vector map of TF-induced direction
    preference shifts (SF\,=\,0.15\,c/deg,
    TF: 1.6\,$\to$\,6.4\,Hz, $n$\,=\,322).}
    Each grey arrow indicates the reference-TF (1.6\,Hz) preferred
    direction rotated to 0$^\circ$; the colored arrow shows the
    comparison-TF (6.4\,Hz) preferred direction after the same
    rotation, with color encoding the signed angular shift
    (colorbar, degrees). Population-level shift statistics are
    summarized in Figure~\ref{fig:polar_summary}.}
  \label{fig:aligned_dir}
\end{figure}

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.6\textwidth]{figures/aligned_oriMap}
  \caption[Aligned delta-vector map: orientation preference]{%
    \textbf{Aligned delta-vector map of TF-induced orientation
    preference shifts.} Same conditions and format as
    Figure~\ref{fig:aligned_dir}, but for orientation preference.
    The majority of colored arrows are near 0$^\circ$, indicating
    that most cells retain a similar orientation preference across
    TF, but a non-negligible fraction show substantial deviations.
    Population-level statistics are shown in
    Figure~\ref{fig:polar_summary}.}
  \label{fig:aligned_ori}
\end{figure}

% Polar summary — both panels side by side, half page
\begin{figure}[htbp]
  \centering

    \includegraphics[width=0.8\textwidth]{figures/shift_radar.pdf}


  \caption[Population polar histograms of TF-induced preference
           shifts]{%
    \textbf{Population-level polar histograms of TF-induced
    direction (left) and orientation (right) preference shifts
    (SF\,=\,0.15\,c/deg, TF: 1.6\,$\to$\,6.4\,Hz).}
    Color encodes the signed angular shift, matching the colorbars
    in Figures~\ref{fig:aligned_dir} and~\ref{fig:aligned_ori}.
    Direction shifts (left) follow a bimodal distribution with
    a primary peak at 0$^\circ$ (stable cells) and a secondary
    peak near 180$^\circ$ (direction-reversing cells), with a
    trough near $\pm$90$^\circ$. Orientation shifts (right) are
    unimodally concentrated near 0$^\circ$ but with a broader
    spread than expected under perfect TF-invariance; the
    distribution is significantly non-uniform (Rayleigh test,
    $p < 0.05$). The qualitative difference between the two
    distributions suggests that direction and orientation
    preference are destabilized by TF changes through partially
    dissociable mechanisms.}
  \label{fig:polar_summary}
\end{figure}

Visual inspection of both aligned maps showed that large and
small shifts coexist throughout the imaged cortical field without
forming discrete ``shift domains.'' No region was exclusively
populated by reversing cells or by stable cells, for either
direction or orientation. Nevertheless, the polar histograms
reveal a qualitatively different pattern for the two features:
direction shifts are bimodally distributed, with a clear reversal
population near 180$^\circ$, while orientation shifts are
unimodally concentrated near 0$^\circ$ but with a non-negligible
spread. This dissociation suggests that the mechanisms driving
TF-induced preference instability differ between the direction
and orientation computations. The spatial intermixing of stable
and unstable cells motivates the pairwise analysis in
Section~\ref{sec:pairwise}, which tests for local clustering
of shift magnitudes more rigorously than visual inspection
allows.

% ----------------------------------------------------------------
\section{Population distribution of preference shifts across SF}
\label{sec:shift_hist}
% ----------------------------------------------------------------

To quantify how the magnitude of TF-induced preference shifts
depends on spatial frequency, we computed, for each neuron at
each SF condition, the absolute angular difference in preferred
direction ($|\Delta\theta_\text{dir}|$, 0--180$^\circ$) and
preferred orientation ($|\Delta\theta_\text{ori}|$, 0--90$^\circ$)
between 1.6 and 6.4\,Hz, and plotted the population distributions
as histograms (Figure~\ref{fig:shift_hist}).

\begin{figure}[htbp]
  \centering
  \includegraphics[width=\textwidth]{figures/_delta_angle_histogram.pdf}
  \caption[Population distributions of TF-induced direction and
           orientation preference shifts across SF]{%
    \textbf{Population distributions of absolute TF-induced
    direction (top, orange) and orientation (bottom, blue)
    preference shifts, for TF: 1.6\,$\to$\,6.4\,Hz, across
    five spatial frequency conditions
    (SF\,=\,0.04--0.64\,c/deg, left to right).}
    Only neurons passing the ANOVA selectivity criterion
    ($p \leq 0.05$) at both TF conditions are included; cell
    counts are given in each panel title.
    \textit{Direction (top)}: All five SF conditions exhibit a
    bimodal distribution, with a primary peak near 0$^\circ$
    (stable cells) and a secondary accumulation near
    150--180$^\circ$ (direction-reversing cells). The trough
    near 90$^\circ$ indicates that intermediate shifts are
    relatively uncommon; cells tend to either remain stable or
    reverse. The secondary peak near 180$^\circ$ is most
    prominent at low SF (0.04\,c/deg) and becomes relatively
    reduced at high SF (0.64\,c/deg),
    suggesting that direction reversals are more prevalent
    when the effective speed of the stimulus is high
    (cf.\ Equation~\ref{eq:speed}).
    \textit{Orientation (bottom)}: At low SF (0.04 and
    0.08\,c/deg), the distribution is strongly right-skewed,
    with most cells showing small shifts but a non-negligible
    tail extending beyond 30--45$^\circ$. As SF increases, the
    distribution becomes progressively flatter, approaching
    uniform at SF\,=\,0.64\,c/deg. This SF-dependent change in
    skewness indicates that the degree of orientation preference
    instability across TF is not uniform across the
    spatiotemporal parameter space.}
  \label{fig:shift_hist}
\end{figure}

% ----------------------------------------------------------------
\section{Pairwise spatial analysis of TF-induced preference shifts}
% ----------------------------------------------------------------
To test whether cells that shift their direction or orientation
preference by similar amounts are spatially clustered, we
performed a pairwise analysis. For every pair of cells within a
given recording, we computed the absolute difference between
their TF-induced preference shifts
($||\Delta\theta_i| - |\Delta\theta_j||$) and the Euclidean
distance separating them on the cortical surface (pixels), then
binned these values into a two-dimensional density heatmap
(Figure~\ref{fig:pairwise}).

\begin{figure}[htbp]
  \centering
  \includegraphics[width=\textwidth]{figures/pairwised_heatmap.pdf}
  \caption[Pairwise cortical distance versus TF-shift difference]{%
    \textbf{Pairwise analysis of cortical distance and TF-induced
    preference shift similarity (SF\,=\,0.15\,c/deg,
    TF: 1.6\,$\to$\,6.4\,Hz, $n$\,=\,322 cells).}
    Each panel is a 2D density heatmap in which the horizontal axis
    is the Euclidean cortical distance between a cell pair (pixels)
    and the vertical axis is the absolute difference between the two
    cells' TF-induced preference shifts. Color encodes the fraction
    of pairs within each cortical-distance bin.
    \textit{Left}: Direction (51{,}000 pairs).
    \textit{Right}: Orientation (51{,}008 pairs).
    At short cortical distances ($<$\,100\,pixels), pairwise shift
    differences are concentrated near zero for both features,
    indicating that nearby cells tend to shift their preference by
    similar amounts when TF changes. This local similarity decays
    with cortical distance and approaches a more uniform
    distribution at separations exceeding $\sim$200--300\,pixels.
    The effect is visible for both direction and orientation,
    though it is most pronounced in the lowest shift-difference
    bins (0--30$^\circ$ for direction; 0--20$^\circ$ for
    orientation). This result indicates that TF-induced preference
    shifts are not random with respect to cortical position: local
    circuit neighbors share similar TF sensitivity, even in the
    absence of discrete ``shift domains'' visible by eye in the
    vector maps of Figures~\ref{fig:aligned_dir}
    and~\ref{fig:aligned_ori}.}
  \label{fig:pairwise}
\end{figure}

The pairwise heatmaps revealed a weak but consistent spatial
structure in TF-induced preference shifts. At short cortical
distances, neighboring cell pairs were more likely to exhibit
similar shift magnitudes than would be expected under spatial
randomness, for both direction and orientation. This spatial
correlation was present across SF conditions, though its
magnitude varied. The effect was modest in absolute terms: the
shift-difference distribution at short distances overlapped
substantially with that at longer distances, indicating that
no cortical neighborhood is exclusively composed of either stable
or unstable cells. Rather, the observed local similarity reflects
a statistical tendency rather than a strict anatomical
segregation, consistent with the visual impression from the
aligned delta-vector maps (Figure~\ref{fig:aligned_dir}, ~\ref{fig:aligned_ori}).

Taken together, the results from Aims~\ref{aim:cellevel}
and~\ref{aim:maplevel} establish that TF-induced changes in
direction and orientation preference in ferret V1 are:
(\textit{i}) widespread at the single-cell level, with direction
reversals occurring in a substantial fraction of neurons;
(\textit{ii}) also present for orientation preference, contrary
to earlier reports of orientation invariance across TF;
(\textit{iii}) weakly but significantly organized on the cortical
surface, such that neighboring neurons tend to shift more
similarly than distant pairs; and (\textit{iv}) dependent on
spatial frequency, with the magnitude and distributional form of
shifts varying systematically with SF.


\chapter{Discussion}

This thesis used two-photon calcium imaging in the
ferret primary visual cortex to characterize, at single-cell
population resolution, how direction and orientation preference
depends on the temporal frequency (TF) of drifting grating
stimuli, and whether TF-induced preference shifts are spatially
organized on the cortical surface. Four principal findings
emerged. First, a substantial fraction of ferret V1 neurons
shifted their preferred direction between low and high TF,
with many cells reversing by nearly 180$^\circ$, replicating 
earlier single-unit observations in ferret V1 \citep{Moore2005} at 
the population-level. Second, and contrary to the prior
conclusion that orientation preference is invariant to TF
\citep{Moore2005}, we observed systematic TF-induced shifts in
preferred orientation as well, accompanied by a broader
TF-dependent decline in orientation selectivity (OSI) that was
not present for direction selectivity (DSI). Third, the
large-scale topology of both direction and orientation maps was
preserved across TF conditions, but pairwise analysis revealed
a weak but consistent local clustering of shift magnitudes:
nearby cells tended to undergo similar TF-induced retuning more
often than distant pairs, without forming discrete ``shift
domains'' visible to the eye. Fourth, the magnitude and
distributional form of these shifts depended on spatial
frequency, with direction reversals most prevalent at low SF
(high effective stimulus speed) and orientation shifts becoming
progressively more uniform as SF increased. Taken together,
these results indicate that direction and orientation
preferences in ferret V1 are not fixed properties of individual
neurons but stimulus-dependent quantities whose variability is
weakly organized in cortical space.

\section{Relationship to prior work on TF-dependent tuning}

The observation that preferred direction varies with TF in a
substantial fraction of ferret V1 neurons extends the findings
of \citet{Moore2005}, who reported that direction selectivity
in ferret area 17 is frequently reduced---and occasionally
reverses---at TFs above or below the preferred. Our densely
sampled two-photon population recapitulates this effect and
additionally reveals its distributional structure: the
bimodal distribution of direction shifts we observed, with a
primary peak at 0$^\circ$ and a secondary peak near
180$^\circ$, is consistent with the interpretation that
TF-induced changes in preferred direction reflect a
categorical reversal in a subset of cells rather than graded
drift across the full range of possible preferences. This
pattern may arise from the underlying spatiotemporal filter
structure of V1 neurons, in which subunits with different TF
tuning dominate the aggregate response at different TF
values, effectively swapping which motion direction drives
the cell most strongly as TF changes
\citep{Adelson1985, Priebe2006}.

Our orientation results require more careful comparison with
\citet{Moore2005}. They reported that orientation-tuning
\emph{bandwidth} (half-width at half-maximum, estimated from
Gaussian fits) is invariant to TF across their sample of
ferret V1 neurons, with mean bandwidths of 20--25$^\circ$ at
every TF tested. Crucially, they did not systematically
examine whether the \emph{preferred} orientation itself
shifts with TF; their analyses focused on bandwidth
stability. Our findings therefore stand in two distinct
relationships to their work. First, the TF-dependent shifts
in preferred orientation we observe constitute a novel
observation rather than a direct contradiction, since this
quantity was not a primary target of their analysis. Second,
the systematic decline in OSI we observe at high TF is in
tension with their bandwidth-invariance result, since a
declining OSI implies broader (or shallower) tuning at high
TF.

Several methodological factors may account for this
divergence, given that both studies used ferret V1. Most
importantly, \citet{Moore2005} performed extracellular
single-unit recordings (n = 32 orientation-tuned neurons),
which biases sampling toward cells with strong, sustained
spiking and clearly resolvable waveforms---typically the most
sharply tuned neurons in the population. Two-photon calcium
imaging samples far more broadly and densely, including cells
with weaker or more heterogeneous tuning that may be
disproportionately susceptible to TF-dependent
destabilization. A second consideration is that broader
tuning curves are inherently more susceptible to apparent
preference shifts, since small changes in the response
profile can translate into larger changes in the preferred
orientation estimated from the circular mean; the OSI
decline and the preference shifts we observe may therefore
be partially coupled phenomena. Whether the orientation
shifts reflect a genuine reorganization of orientation
coding, a measurement consequence of broadened tuning at
high TF, or some combination of the two cannot be fully
resolved by the present analyses and is an important target
for future work.

\section*{Spatial organization of preference shifts}

A novel contribution of this thesis is the characterization of
the spatial organization of TF-induced preference shifts across
the cortical surface. Two complementary observations emerged.
On one hand, the large-scale topology of the direction and
orientation maps was preserved between 1.6 and 6.4\,Hz: smooth
gradients, fracture lines, and iso-orientation domains were
visible at both TF conditions, consistent with the classical
columnar architecture of ferret V1
\citep{Bonhoeffer1991, Weliky1996}. On the other hand,
pairwise analysis revealed that nearby cells were more likely
to share similar shift magnitudes than distant cells, a finding
that visual inspection of the aligned delta-vector maps alone
would have missed. The coexistence of these two observations
suggests that TF-dependent retuning operates \textit{within}
the existing functional architecture rather than disrupting it:
the underlying columnar layout remains intact, but local
neighborhoods of cells share a common susceptibility to
TF-induced shifts. This is consistent with a view in which
local cortical circuits, rather than individual cells in
isolation determine how preference varies with stimulus
parameters, possibly through shared thalamocortical input or
shared local recurrent connectivity \citep{Nauhaus2008}.

The fact that no discrete ``shift domains'' were visible by
eye, despite a statistically detectable pairwise correlation,
may indicate that the spatial clustering of shifts is a
statistical tendency rather than a strict anatomical
segregation. This weak-but-consistent organization is
reminiscent of other sub-map structures in V1, such as the
fine-scale clustering of spatial frequency preference and the
cone-shaped geometry of orientation domains revealed by recent
functional ultrasound imaging \citep{Hu2023}. One possibility
is that TF-dependent preference shifts reflect the spatial
structure of some underlying functional map that is not itself
visualized here---most notably, the recently described
functional architecture for stimulus speed in ferret V1
\citep{Casanova2025}, in which speed-tuned neurons form
spatially clustered domains. If cells in a speed domain are
driven by TF/SF inputs that are preferentially coupled to a
particular motion direction, the TF-induced reversal of
preferred direction we observe may itself be a signature of the
speed-tuning machinery being probed through a TF-only slice of
the stimulus parameter space. Testing this conjecture directly
will require jointly mapping speed preference and TF-induced
direction shifts in the same cortical field---a natural
extension of the analyses presented here, but one that lies
beyond the scope of this thesis. At the very least, if there is a map structural basis of the dependence of direction and orientation on TF and SF, it is not apparent yet.

\section{Implications for the concept of V1 functional maps}

The classical picture of V1 functional maps treats preferred
direction and preferred orientation as labels attached to
individual neurons---properties measured once, at a single
stimulus condition, and treated as stable. The present results
argue for a revised view: preferences are not labels but
measurements, each conditioned on the stimulus used to probe
them. A cell that prefers rightward motion at 1.6\,Hz may
prefer leftward motion at 6.4\,Hz; a cell that prefers a
horizontal edge at one TF may prefer a tilted edge at another.
The functional map obtained at any single TF is therefore one
slice through a higher-dimensional, stimulus-dependent tuning
landscape, and the weak spatial organization of shifts
observed here suggests that the cortex is structured not only
to represent these preferences, but also to represent their
stimulus dependence in a spatially coherent way.

This perspective does not contradict the classical concept of
columnar functional architecture that the gross topology of the
direction and orientation maps is preserved across TF
conditions. What is preserved across TF is
the scaffolding: the smooth large-scale gradients, the fracture
lines, the iso-orientation domains. What varies is the
fine-scale assignment of individual cells to preferences within
that scaffolding, and this variation is not uniform across
cortex but is locally shared among neighbors. A complete
characterization of V1 functional organization therefore
requires not only single-stimulus maps but also the
stimulus-dependence of those maps, and not only single-cell
tuning curves but also the spatial correlation structure of
how those tuning curves vary with stimulus parameters.

\section{Limitations}

Several limitations of this work should be noted. First, the
main figures presented here are drawn from a single
representative animal (Ferret 365 [A], imaged 2020-09-04) and
a single spatial frequency (0.15\,c/deg) for the map-level and
pairwise analyses. While the single-cell shift histograms
(Figure~\ref{fig:shift_hist}) span all five SF conditions, the
spatial analyses focus on the SF at which tuning is strongest
in ferret V1. Cross-animal consistency is addressed in the
Supplementary Material, but a quantitative comparison of map
topology and pairwise structure across all seven ferrets is a
natural extension that would strengthen the generality of the
conclusions.

Second, two-photon calcium imaging reports a slow, nonlinear
proxy for spiking activity. Small differences in tuning at
high TF, where individual responses are smaller and noisier,
may be underestimated or obscured by the indicator dynamics.
The TF-dependent decline in OSI observed here could, in
principle, reflect such measurement effects in addition to
genuine broadening of orientation tuning, though the
qualitative pattern such as direction reversals, orientation shifts,
local pairwise clustering is robust to this concern because
it depends on \textit{changes} in preference rather than
absolute tuning strength.

Third, the analyses presented here operate on preferred
direction and preferred orientation as computed from the
circular mean of the tuning curve. For cells with broad or
multi-peaked tuning, this summary statistic can be unstable,
and small changes in the underlying response profile can
produce large apparent shifts. A more complete future analysis
would model the full tuning curve at each TF condition and
classify cells by the nature of their TF-dependence
(stable, reversing, broadening, etc.) rather than by a single
scalar shift magnitude.

Fourth, this thesis does not pursue the joint SF$\times$TF tuning 
structure or manifold-based population analyses that would be needed to connect these
observations directly to the question of speed encoding. The
relationship between TF-induced direction reversals and the
speed-tuning architecture recently described by
\citet{Casanova2025} is a compelling next step but is
not addressed here.

\section{Future directions}

The findings of this thesis suggest several immediate
extensions. First, a joint analysis of preferred speed and
TF-induced direction shifts in the same cortical field would
test whether the direction-reversing population is
preferentially located within speed-tuned domains. Second,
extending the pairwise spatial analysis to the full five-SF
datasets, and to all seven animals, would establish the
robustness and SF-dependence of the local clustering effect.
Third, population-level dimensionality reduction (e.g., UMAP
or related manifold methods) applied to the full direction
$\times$ SF $\times$ TF response tensor could reveal whether
cells that shift their preferences together lie on shared
low-dimensional subspaces of the population activity, which is a
structural signature that scalar shift analyses cannot detect.
Finally, the mechanisms that produce TF-dependent preference
shifts (whether they arise from complex-cell spatiotemporal
filter structure, from subcortical inputs with TF-dependent
direction biases, or from local recurrent circuits) remain 
to be identified and will require targeted perturbation
experiments to dissect.

\section{Conclusion}

This thesis establishes that direction and orientation
preferences in ferret V1 are not fixed properties of individual
neurons but vary systematically with the temporal frequency
of the probe stimulus, and that these TF-induced shifts are
weakly but consistently organized in cortical space. The
classical columnar architecture of V1 is preserved across TF,
but within that scaffolding, the fine-scale assignment of
cells to direction and orientation preferences is
stimulus-dependent, and the stimulus dependence is itself
spatially structured. These results argue for a revised view
of V1 functional maps as stimulus conditioned structures
rather than fixed labels, and they provide a population level,
spatially resolved foundation for future work connecting
direction and orientation tuning to the broader question of
how V1 encodes motion and speed.



% Put references in a singlespacing environment so it goes back to double for the appendices
\begin{singlespacing}
\bibliographystyle{plainnat}
\bibliography{main}
\end{singlespacing}


\appendix
\chapter{Supplementary Material}

% ----------------------------------------------------------------
% Supplementary Figure S1: Direction maps across animals
% ----------------------------------------------------------------
\begin{figure}[htbp]
    \centering
    \includesvg[width=\textwidth]{figures/driMap.svg}
    \caption[Direction preference maps across all animals]{%
    \textbf{Direction preference maps at 1.6 and 6.4\,Hz for all
    imaged animals (SF\,=\,0.15\,c/deg).}
    Each panel shows the direction preference map for one recording
    session, with each arrow indicating the preferred motion
    direction (0--360$^\circ$) of a responsive cell at its cortical
    location. The low-TF condition is always 1.6\,Hz and the
    high-TF condition is always 6.4\,Hz; the two TF conditions are
    shown in different colors within each panel. Ferret 356 (imaged
    2019-11-12) is not shown because an insufficient number of
    cells passed the ANOVA responsiveness criterion at both TF
    conditions to support a meaningful map-level analysis. For the
    session on 2020-08-21, a different color pair is used because
    only a reduced set of TF conditions was recorded in that
    session; however, the two TF values displayed are the same as
    those used throughout the rest of the thesis. Across the six
    remaining sessions, the hallmark features of ferret V1 direction
    organization---smooth gradients of preferred direction
    punctuated by sharp fracture lines---are visible at both TF
    conditions, with varying degrees of local disagreement between
    the two TFs. The session shown in the main text
    (Figure~\ref{fig:dir_maps}) corresponds to 2020-09-04.}
    \label{fig:all_dirMaps}
\end{figure}

% ----------------------------------------------------------------
% Supplementary Figure S2: Orientation maps across animals
% ----------------------------------------------------------------
\begin{figure}[htbp]
    \centering
    \includesvg[width=\textwidth]{figures/oriMap.svg}
    \caption[Orientation preference maps across all animals]{%
    \textbf{Orientation preference maps at 1.6 and 6.4\,Hz for all
    imaged animals (SF\,=\,0.15\,c/deg).}
    Same format and conditions as
    Supplementary Figure~\ref{fig:all_dirMaps}, but for preferred
    orientation ($-90^\circ$ to $90^\circ$). Each cell is represented
    by a line segment oriented parallel to its preferred orientation
    at the indicated TF. As in the direction maps, Ferret 356 is
    omitted due to insufficient responsive cells at both TFs, and
    the 2020-08-21 session uses a distinct color pair because a
    reduced TF set was recorded in that session (the comparison
    remains 1.6 vs. 6.4\,Hz throughout). Across sessions, smooth
    gradients of preferred orientation are visible at both TF
    conditions, consistent with the well-described columnar
    orientation architecture of ferret V1
    \citep{Bonhoeffer1991, Weliky1996}. Local disagreements between
    the two TF conditions are visible in every session, supporting
    the generality of the TF-dependent orientation preference
    shifts described in the main text.}
    \label{fig:all_oriMaps}
\end{figure}

% ----------------------------------------------------------------
% Supplementary Figure S3: Pooled polar histograms across animals
% ----------------------------------------------------------------
\begin{figure}[htbp]
    \centering
    \includegraphics[width=\textwidth]{figures/summary_shift.pdf}
    \caption[Pooled polar histograms of TF-induced preference shifts
    across animals]{%
    \textbf{Pooled population-level polar histograms of TF-induced
    direction (left) and orientation (right) preference shifts,
    aggregated across six recording sessions (SF\,=\,0.15\,c/deg,
    TF: 1.6\,$\to$\,6.4\,Hz, $n$\,=\,1{,}577 cells).}
    Cells from all six sessions with sufficient responsive
    populations were pooled, and the polar histograms summarize the
    signed angular shift in preferred direction and preferred
    orientation between 1.6 and 6.4\,Hz. Color encodes the signed
    angular shift, matching the conventions used in the main-text
    population histograms (Figure~\ref{fig:polar_summary}).
    The pooled direction distribution preserves the bimodal
    structure observed in the representative animal, with a
    primary peak at 0$^\circ$ (stable cells) and a clear secondary
    peak near 180$^\circ$ (direction-reversing cells); the pooled
    orientation distribution remains unimodally concentrated near
    0$^\circ$ but with a non-negligible spread. The consistency of
    these population-level signatures across animals indicates that
    the TF-induced shifts described in the main text are not
    idiosyncratic to the representative session but reflect a
    general property of ferret V1.}
    \label{fig:summary_shift}
\end{figure}

% ----------------------------------------------------------------
% Supplementary Figure S4: Cross-SF pooled shift histograms
% ----------------------------------------------------------------
\begin{figure}[htbp]
    \centering
    \includegraphics[width=\textwidth]{figures/allSF_shift_histogram.pdf}
    \caption[Pooled shift histograms across SF conditions and
    animals]{%
    \textbf{Pooled population distributions of absolute TF-induced
    direction (top, orange) and orientation (bottom, blue)
    preference shifts across all SF conditions and animals, for
    TF: 1.6\,$\to$\,6.4\,Hz.}
    Data from all six sessions with sufficient responsive
    populations are pooled within each SF condition
    (SF\,=\,0.04--0.64\,c/deg, left to right). Only cells passing
    the ANOVA selectivity criterion ($p \leq 0.05$) at both TF
    conditions are included; cell counts are given in each panel
    title ($n$\,=\,1{,}577--1{,}772 per SF). Histograms show the
    proportion of cells falling in each angular shift bin.
    \textit{Direction (top)}: The bimodal distribution observed in
    the representative animal (Figure~\ref{fig:shift_hist}) is
    preserved in the pooled dataset, with a primary peak near
    0$^\circ$ and a secondary accumulation near 150--180$^\circ$.
    The secondary reversal peak is most prominent at low SF
    (0.04--0.08\,c/deg) and becomes relatively reduced at high SF
    (0.32--0.64\,c/deg), consistent with the SF-dependence
    described in the main text.
    \textit{Orientation (bottom)}: At low SF (0.04 and
    0.08\,c/deg), the distribution is strongly right-skewed,
    with most cells showing small shifts but a tail extending to
    larger values. As SF increases, the distribution becomes
    progressively flatter, approaching uniform at the highest SF
    conditions. The qualitative patterns observed in the
    representative animal generalize across the pooled population
    of cells from all six sessions.}
    \label{fig:allSF_shift_histogram}
    
\end{figure}
\end{document}
